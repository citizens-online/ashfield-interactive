tmap_options_reset()
tmap_options(
  title.fontface = 2,
  title.size = 1.4,
  main.title.fontface = 2,
  main.title.size = 1,
  main.title.color = "black",
  main.title.position = "LEFT",
  frame = "grey50",
  # frame = FALSE,
  legend.show = TRUE,
  legend.outside = FALSE,
  # asp = 0.67,
  legend.outside.size = 0.25,
  # legend.outside.position = "top",
  # legend.height = 1,
  legend.position = c("right", "top"),
  # legend.position = c("RIGHT", "TOP"),
  # legend.width = 0.2,
  legend.title.fontface = 2,
  # legend.title.size = 4,
  legend.text.size = 0.8,
  legend.bg.color = "white",
  # legend.bg.alpha = 0.9,
  legend.frame = "grey50",
  attr.outside = TRUE,
  # attr.outside.position = "bottom",
  # attr.outside.size = 0.07,
  bg.color = "white",
  outer.bg.color = "white",
  inner.margins = 0.05,
  outer.margins = 0.02,
  unit = "mi",
  # basemaps = leaflet::providers$Hydda.Base,
  # basemaps = "https://{s}.tile.openstreetmap.se/hydda/base/{z}/{x}/{y}.png",
  basemaps = leaflet::providers$CartoDB.Positron,
  # basemaps = leaflet::providers$CartoDB.VoyagerLabelsUnder,
  # basemaps = leaflet::providers$CartoDB.Voyager,
  basemaps.alpha = 1,
  # overlays = leaflet::providers$Hydda.RoadsAndLabels,
  # overlays = "https://{s}.tile.openstreetmap.se/hydda/roads_and_labels/{z}/{x}/{y}.png",
  # overlays = leaflet::providers$OpenMapSurfer.Hybrid,
  # overlays = leaflet::providers$OpenMapSurfer.AdminBounds,
  overlays.alpha = 0.5,
  output.size = 70,
  # output.dpi = 600,
  fontfamily = "Source Sans Pro"
)

# district_hydda_base <- tmaptools::read_osm(district_boundary, type = "http://c.tile.openstreetmap.se/hydda/base/{z}/{x}/{y}.png")
# ef_hydda_roads <- tmaptools::read_osm(epping_oas, type = "http://f.tile.openstreetmap.se/hydda/roads_and_labels/{z}/{x}/{y}.png")
# ef_hydda_full <- tmaptools::read_osm(ef_bbox, type = "http://f.tile.openstreetmap.se/hydda/full/{z}/{x}/{y}.png")
